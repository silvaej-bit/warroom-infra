# Adicione estas chaves ao Secret 'weeeeeekend-secrets' no namespace weeeeeekend-dev
# Arquivo: kubernetes/environments/dev/secrets.yml (merge com as chaves existentes)
#
# Substitua os valores antes de aplicar:
#   kubectl apply -f kubernetes/environments/dev/secrets.yml

WARROOM_DB_URL: "jdbc:postgresql://<HOST_DEV>:5432/warroom"
WARROOM_DB_USER: "warroom_dev"
WARROOM_DB_PASSWORD: "CHANGE_ME"
