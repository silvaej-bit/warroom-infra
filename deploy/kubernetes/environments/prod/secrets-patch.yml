# Adicione estas chaves ao External Secrets em prod
# Arquivo: kubernetes/environments/prod/secrets.yml (merge com as chaves existentes)
#
# Em prod, as credenciais devem ser gerenciadas pelo External Secrets Operator
# (AWS Secrets Manager / Azure Key Vault / GCP Secret Manager)

WARROOM_DB_URL: "MANAGED_BY_EXTERNAL_SECRETS"
WARROOM_DB_USER: "MANAGED_BY_EXTERNAL_SECRETS"
WARROOM_DB_PASSWORD: "MANAGED_BY_EXTERNAL_SECRETS"
